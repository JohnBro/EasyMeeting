<local:TransparentWindow x:Class="EasyMeeting.UI.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EasyMeeting.UI.Controls"
        Title="Preview" 
        Height="300" 
        Width="400"
        Topmost="True"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        ShowInTaskbar="False">
    <Border Background="#E0202020" CornerRadius="10" Padding="10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Live Transcription" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,5"/>
            
            <TextBox Grid.Row="1" 
                     x:Name="PreviewText"
                     Text="{Binding Transcript}"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     IsReadOnly="True"
                     Background="Transparent"
                     Foreground="White"
                     BorderThickness="0"
                     FontSize="14"
                     FontFamily="Segoe UI"/>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Size:" Foreground="#888888" FontSize="11" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Slider x:Name="FontSizeSlider" Width="80" Minimum="10" Maximum="24" Value="14" 
                        VerticalAlignment="Center" ValueChanged="FontSizeSlider_ValueChanged"/>
                <TextBlock Text="Opacity:" Foreground="#888888" FontSize="11" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <Slider x:Name="OpacitySlider" Width="80" Minimum="30" Maximum="100" Value="80" 
                        VerticalAlignment="Center" ValueChanged="OpacitySlider_ValueChanged"/>
            </StackPanel>
        </Grid>
    </Border>
</local:TransparentWindow>
